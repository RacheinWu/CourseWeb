<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.he.ssm.dao.other.TeacherDao">
    <!--字段映射-->
    <resultMap id="BaseResultMap" type="com.he.ssm.entity.other.Teacher">
        <id property="id"    column="id"    />
        <result property="teacherType"    column="teacherType"    />
        <result property="teacherName"    column="teacherName"    />
        <result property="title"    column="title"    />
        <result property="post"    column="post"    />
        <result property="email"    column="email"    />
        <result property="photoUrl"    column="photoUrl"    />
        <result property="content"    column="content"    />
        <result property="state"    column="state"    />
        <result property="createDate"    column="createDate"    />
        <result property="publishDate"    column="publishDate"    />
        <result property="honor"    column="honor"    />
    </resultMap>
    <!--所有字段-->
    <sql id="Base_Column_List">
         id, teacherType, teacherName, title, post, email, photoUrl, content, state, createDate, publishDate ,honor   </sql>
    <!--插入-->
    <insert id="insert" parameterType="com.he.ssm.entity.other.Teacher" keyColumn="id" keyProperty="id" useGeneratedKeys="true">
        insert into teacher(teacherType,teacherName,title,post,email,photoUrl,content,state,createDate,publishDate,honor)
        values(#{teacherType},#{teacherName},#{title},#{post},#{email},#{photoUrl},#{content},#{state},#{createDate},#{publishDate},#{honor})
    </insert>
    <!--删除-->
    <delete id="deleteByPrimaryKey" parameterType="Long">
        delete from teacher where id = #{id}
    </delete>
    <!--删除-->
    <delete id="deleteByPrimaryKeyIn">
        delete from teacher where id in
        <foreach collection="idList" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    <!--更新-->
    <update id="updateByPrimaryKey" parameterType="com.he.ssm.entity.other.Teacher">
        update teacher set teacherType = #{teacherType}, teacherName = #{teacherName}, title = #{title}, post = #{post}, email = #{email}, photoUrl = #{photoUrl}, content = #{content}, state = #{state}, createDate = #{createDate}, publishDate = #{publishDate}, honor = #{honor}        where id = #{id}
    </update>
    <!--查询-->
    <select id="selectByPrimaryKey" parameterType="Long" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from teacher
        where id = #{id}
    </select>
    <!--查询-->
    <select id="findByCondition" parameterType="com.he.ssm.entity.other.Teacher" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from teacher
        <where>  
            <if test="teacherType != null  and teacherType != ''"> and teacherType = #{teacherType}</if>
            <if test="teacherName != null  and teacherName != ''"> and teacherName like concat('%', #{teacherName}, '%')</if>
            <if test="state != null  and state != ''"> and state = #{state}</if>
            <if test="beginPublishDate != null"> and publishDate <![CDATA[ >= ]]> #{beginPublishDate}</if>
            <if test="endPublishDate != null"> and publishDate <![CDATA[ <= ]]> #{endPublishDate}</if>
        </where>
    </select>
    <!--查询-->
    <select id="findByAll" parameterType="com.he.ssm.entity.other.Teacher" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from teacher
        <where>
            <if test="teacherType != null  and teacherType != ''"> and teacherType = #{teacherType}</if>
            <if test="teacherName != null  and teacherName != ''"> and teacherName =#{teacherName}</if>
            <if test="state != null  and state != ''"> and state = #{state}</if>
            <if test="beginPublishDate != null"> and publishDate <![CDATA[ >= ]]> #{beginPublishDate}</if>
            <if test="endPublishDate != null"> and publishDate <![CDATA[ <= ]]> #{endPublishDate}</if>
        </where>
    </select>
    <!--auto generated by MybatisCodeHelper on 2021-03-09-->
    <update id="updateStateById">
        update teacher
        set `state`=#{updatedState},publishDate=#{publishDate}
        where id=#{id}
    </update>

<!--auto generated by MybatisCodeHelper on 2021-03-11-->
    <select id="findByStateOrderByPublishDateDesc" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from teacher
        where `state`=#{state}
        <if test="search != null  and search != ''"> and (teacherName like concat('%', #{search}, '%') )</if>
        <if test="honor != null  and honor != ''"> and honor=#{honor}</if>
        order by publishDate desc
    </select>
</mapper>