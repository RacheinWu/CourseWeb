<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.he.ssm.dao.other.MsgtitleDao">
    <!--字段映射-->
    <resultMap id="BaseResultMap" type="com.he.ssm.entity.other.Msgtitle">
        <id property="id"    column="id"    />
        <result property="title"    column="title"    />
        <result property="state"    column="state"    />
        <result property="createDate"    column="createDate"    />
    </resultMap>
    <!--所有字段-->
    <sql id="Base_Column_List">
         id, title, state, createDate    </sql>
    <!--插入-->
    <insert id="insert" parameterType="com.he.ssm.entity.other.Msgtitle" keyColumn="id" keyProperty="id" useGeneratedKeys="true">
        insert into msgtitle(title,state,createDate)
        values(#{title},#{state},#{createDate})
    </insert>
    <!--删除-->
    <delete id="deleteByPrimaryKey" parameterType="Long">
        delete from msgtitle where id = #{id}
    </delete>
    <!--删除-->
    <delete id="deleteByPrimaryKeyIn">
        delete from msgtitle where id in
        <foreach collection="idList" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    <!--更新-->
    <update id="updateByPrimaryKey" parameterType="com.he.ssm.entity.other.Msgtitle">
        update msgtitle set title = #{title}, state = #{state}, createDate = #{createDate}        where id = #{id}
    </update>
    <!--查询-->
    <select id="selectByPrimaryKey" parameterType="Long" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from msgtitle
        where id = #{id}
    </select>
    <!--查询-->
    <select id="findByCondition" parameterType="com.he.ssm.entity.other.Msgtitle" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from msgtitle
        <where>  
            <if test="title != null  and title != ''"> and title like concat('%', #{title}, '%')</if>
            <if test="state != null  and state != ''"> and state = #{state}</if>
        </where>
    </select>
    <!--查询-->
    <select id="findUnique" parameterType="com.he.ssm.entity.other.Msgtitle" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/> from msgtitle
        <where>
    <if test="title != null  and title != ''"> and title = #{title}</if>
    <if test="state != null  and state != ''"> and state = #{state}</if>
        </where>
    </select>

<!--auto generated by MybatisCodeHelper on 2021-03-21-->
    <update id="updateStateById">
        update msgtitle
        set `state`=#{updatedState}
        where id=#{id}
    </update>

<!--auto generated by MybatisCodeHelper on 2021-03-21-->
    <select id="findByState" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from msgtitle
        where `state`=#{state}
    </select>
</mapper>